{
  "name": "bedayah2030",
  "private": true,
  "version": "0.1.0",
  "description": "نظام بداية لإدارة الأعمال - Bedayah Business Management System",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:api": "npm run start:dev --workspace=apps/api",
    "dev:web": "npm run dev --workspace=apps/web",
    "build": "npm run build --workspace=packages/shared && npm run build --workspace=packages/db && npm run build --workspace=apps/api && npm run build --workspace=apps/web",
    "test": "npm run test --workspace=apps/api",
    "prisma:generate": "npm run generate --workspace=packages/db",
    "prisma:migrate": "npm run migrate --workspace=packages/db",
    "prisma:migrate:deploy": "npm run migrate:deploy --workspace=packages/db",
    "prisma:seed": "npm run seed --workspace=packages/db",
    "lint": "npm run lint --workspace=apps/api && npm run lint --workspace=apps/web"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "UNLICENSED"
}
